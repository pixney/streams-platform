{% for row in rows %}
    <div class="field-group">
        <h1 class='errorchecker'>/Users/pixney/Development/Web/Ryan/vendor/anomaly/streams-platform/resources/views/form/partials/rows.twig</h1>
        <div class="row">
            {% for column in row.columns %}
                <div class="{{ column.classes ? column.classes|join(' ') : 'col-lg-' ~ (column.size ?: 24) }}">

                    {% if column.fields %}
                        {% include "streams::form/partials/fields" with {"fields": column.fields} %}
                    {% elseif column.html %}
                        {{ parse(column.html)|raw }}
                    {% elseif column.view %}
                        {% include column.view %}
                    {% endif %}

                </div>
            {% endfor %}
        </div>
    </div>
{% endfor %}
